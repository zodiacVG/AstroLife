# FastAPI 服务配置
HOST=0.0.0.0
PORT=8000

# 阿里云百炼（可选，如需启用 LLM 能力）
ALIYUN_BAILIAN_API_KEY=
ALIYUN_BAILIAN_MODEL=qwen-plus

# CORS 允许来源（逗号分隔）。为空则默认允许本机常用端口。
CORS_ALLOW_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# 数据库连接（可选）
# - 开发默认使用 SQLite（无需填写，代码会落到 backend/data/app.db）
# - 生产（Zeabur）建议使用托管 Postgres/MySQL，并在此提供连接串
# 例：Postgres -> DATABASE_URL=postgresql+psycopg2://user:pass@host:5432/dbname
# DATABASE_URL=
