# FastAPI 服务配置
HOST=0.0.0.0
PORT=8000

# 阿里云百炼（可选，如需启用 LLM 能力）
ALIYUN_BAILIAN_API_KEY=
ALIYUN_BAILIAN_MODEL=qwen-plus

# CORS 允许来源（逗号分隔）。为空则默认允许本机常用端口。
# 方式一：显式列出允许的来源（逗号分隔）
CORS_ALLOW_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# 方式二：使用正则（可选）。未设置时，后端默认放行本机与私网网段：
#   localhost/127.0.0.1，10.x.x.x，172.16-31.x.x，192.168.x.x 任意端口
# CORS_ALLOW_ORIGIN_REGEX=^http://(localhost|127\.0\.0\.1|10\.[0-9.]+|172\.(1[6-9]|2[0-9]|3[0-1])\.[0-9.]+|192\.168\.[0-9.]+):\d+$

# 无需数据库配置：历史记录保存在浏览器 localStorage。
